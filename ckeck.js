const numbers = prompt ('Введите число');
const number = prompt ('Введите число');

if ( numbers > number ) {
    alert( number + ' less ' + numbers)
}else if (numbers < number) {
    alert( number + ' more ' + numbers)
} else{
    alert ('равны')
}
    